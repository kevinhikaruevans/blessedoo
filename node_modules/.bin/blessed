#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../blessed/bin/tput.js" "$@"
  ret=$?
else 
  node  "$basedir/../blessed/bin/tput.js" "$@"
  ret=$?
fi
exit $ret
